version: '3'
services:
  webapp:
    build: .
    container_name: webapp_jenkins_build
    ports:
      - "8081:80"  # Using different port than Part-I
    volumes:
      - ./:/app  # Mounting code as volume
    networks:
      - webapp-network

networks:
  webapp-network:
    driver: bridge
